# Build output
/bin/
/dist/
*.exe

# Local configuration and secrets
.env
config/servers.local.json

# Session logs
/logs/

# Editor / OS
.DS_Store
*.swp

# Sandbox the MCP servers operate on
/workspace/

# Frame logs are per-run artefacts
/logs/

# Locally built helper binaries
/bin/

# BrewOps is its own module and its own PUBLIC repository. This one is private,
# and a public module cannot depend on a private one, so the two never merge.
# Ignored here so an "add ." in this repository can never swallow it.
/brewops/
